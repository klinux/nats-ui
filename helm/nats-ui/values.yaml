replicaCount: 1

image:
  repository: klinux/nats-ui
  pullPolicy: IfNotPresent
  tag: ""  # defaults to appVersion

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# Application configuration
config:
  port: "8080"
  baseURL: ""  # auto-generated from ingress if empty
  natsURL: "nats://nats:4222"
  natsUser: "admin"
  adminUser: "admin"
  # Comma-separated proxy IPs/CIDRs allowed to set X-Forwarded-For. Empty means
  # trust none — set this to your ingress range so per-IP rate limiting sees
  # real client addresses instead of the ingress pod's.
  trustedProxies: ""
  # Max object-store upload size in bytes (default 104857600 = 100 MiB).
  maxUploadSize: ""
  rateLimitRPS: ""
  corsOrigins: ""

# Sensitive values — stored in a Secret
secrets:
  natsPass: ""
  # Set a real password. Left empty, the chart generates one on first install
  # and reuses it on upgrades; read it from the Secret to log in.
  adminPass: ""
  # Left empty, a random secret is generated on first install and preserved
  # across upgrades so existing sessions stay valid.
  jwtSecret: ""

# OAuth2 configuration (optional)
oauth2:
  google:
    clientID: ""
    clientSecret: ""
  github:
    clientID: ""
    clientSecret: ""
  keycloak:
    url: ""           # e.g. https://keycloak.example.com
    realm: "master"
    clientID: ""
    clientSecret: ""
  oidc:
    name: "SSO"       # display name on login page
    issuerURL: ""      # OIDC issuer URL (uses .well-known/openid-configuration)
    clientID: ""
    clientSecret: ""
    scopes: "openid email profile"
  allowedUsers: "*"  # comma-separated emails or * for all

# Use an existing secret instead of creating one
# The secret must contain all sensitive keys (see secret.yaml for the full list)
existingSecret: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}

securityContext:
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: nats-ui.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

# Kubernetes Gateway API (alternative to ingress)
gateway:
  enabled: false
  annotations: {}
  parentRefs:
    - name: default-gateway
      # namespace: gateway-ns
      # sectionName: https
      # port: 443
  hostnames: []
    # - nats-ui.example.com
  rules: []
    # - matches:
    #     - path:
    #         type: PathPrefix
    #         value: /
    #   filters: []

resources: {}

livenessProbe:
  httpGet:
    path: /api/health
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /api/health
    port: http
  initialDelaySeconds: 3
  periodSeconds: 5

nodeSelector: {}
tolerations: []
affinity: {}
